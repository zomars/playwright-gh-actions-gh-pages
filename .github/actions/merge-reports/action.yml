name: Merge playwright HTML reports
description: "Downloads blobs and generate a single HTML report"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 18
    - name: Install dependencies
      run: yarn
      shell: bash
    - name: Download blob reports from GitHub Actions Artifacts
      uses: actions/download-artifact@v4
      with:
        path: all-blob-reports
        pattern: blob-report-*
        merge-multiple: true
    - name: Merge into HTML Report
      run: yarn playwright merge-reports --reporter html ./all-blob-reports
      shell: bash
    - name: Upload HTML report
      uses: actions/upload-artifact@v4
      with:
        name: html-report--attempt-${{ github.run_number }}-${{ github.run_attempt }}
        path: playwright-report
        retention-days: 14
